node_modules/
.next/
.env*.local
.vercel
tsconfig.tsbuildinfo

# Per-user local Claude Code settings (session permission grants, etc.)
.claude/settings.local.json

# Real people's voices — stays local
test-clips/*.wav
test-clips/*.m4a

# Generated by scripts/eleven-ttfa-probe.mjs — reproducible, not worth committing
test-clips/eleven-probe-out/

# Eval run outputs — timestamped, not worth committing
eval-results/

# Real device captures for the English-clause investigation — same reasoning
# as test-clips/*.wav above (real people's voices, stays local); that rule is
# directory-local and doesn't cover this subdirectory, hence a separate line.
test-clips/english-clause/*.wav

# scripts/english-clause-eval.mjs run outputs — timestamped, not worth committing
test-clips/english-clause/baseline-*.json

# TTS listening-eval audio — generated, and contains synthesized speech
eval-audio/
